<div class="section">
  <div class="container">
    <div class="row">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'trends' %>
      <% end %>
      <%= render 'tweet' %>
      <%= render 'suggested-follows' %>
      <%= render 'feed' %>
    </div>
  </div>
</div>
